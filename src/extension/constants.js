/** Extension-wide identifiers kept in one place to prevent string drift. */
const VIEW_TYPE = 'desmos.graph';
const COMMAND_OPEN_CALCULATOR = 'vscode-desmos.openCalculator';

module.exports = { VIEW_TYPE, COMMAND_OPEN_CALCULATOR };
